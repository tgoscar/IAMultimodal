# Qué va aquí
- Tabla de `mine_hard_negatives(sim, metadata, top_n=8)` (C9 §8) exportada a CSV
- Imagen del hard negative principal con su caption incorrecta (captura o `savefig`)
- 2-3 líneas de interpretación: qué comparten el positivo y el negativo (objetos/escena) y qué los distingue (composición)
